# Revisions ignored by `git blame` — bulk, no-behaviour-change reformatting only.
# Enable locally: git config blame.ignoreRevsFile .git-blame-ignore-revs
# Appended by a follow-up PR once the reformat merges; one full SHA, with a comment.
# Everything above this line is a seed; everything below is this repository's history.
